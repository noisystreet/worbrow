name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    name: build (${{ matrix.asset }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            asset: worbrow-linux-x86_64
            suffix: ''
            ext: tar.gz
            deb: true
          # musl 静态链接：不依赖 glibc，可在任何 Linux 发行版/容器直接运行（Rust CLI 标准发布形态）
          - os: ubuntu-latest
            asset: worbrow-linux-x86_64-musl
            suffix: ''
            ext: tar.gz
            musl: true
          - os: macos-latest
            asset: worbrow-macos-arm64
            suffix: ''
            ext: tar.gz
          - os: windows-latest
            asset: worbrow-windows-x86_64.exe
            suffix: '.exe'
            ext: zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: install musl target
        if: matrix.musl
        run: |
          rustup target add x86_64-unknown-linux-musl
          sudo apt-get update
          sudo apt-get install -y musl-tools

      # release 构建即完整性验证（--locked 锁定锁文件；全量门禁已由 PR 的 ci.yml 覆盖）
      - name: build release binary
        run: cargo build --release --all-features --locked ${{ matrix.musl && '--target x86_64-unknown-linux-musl' || '' }}

      - name: stage binary
        shell: bash
        run: |
          mkdir -p dist
          BIN="target/release/worbrow${{ matrix.suffix }}"
          if [ "${{ matrix.musl }}" = "true" ]; then BIN="target/x86_64-unknown-linux-musl/release/worbrow"; fi
          cp "$BIN" "dist/${{ matrix.asset }}"
          # 静态链接自证：musl 产物不应依赖动态库
          if [ "${{ matrix.musl }}" = "true" ]; then file "$BIN"; fi

      - name: package (tar.gz)
        if: matrix.ext == 'tar.gz'
        run: tar -C dist -czf "dist/${{ matrix.asset }}.tar.gz" "${{ matrix.asset }}"

      - name: package (zip)
        if: matrix.ext == 'zip'
        shell: pwsh
        run: Compress-Archive -Path "dist/${{ matrix.asset }}" -DestinationPath "dist/${{ matrix.asset }}.zip"

      - uses: taiki-e/install-action@cargo-deb
        if: matrix.deb

      - name: build deb
        if: matrix.deb
        run: cargo deb

      - name: stage deb
        if: matrix.deb
        shell: bash
        run: cp target/debian/*.deb dist/

      - name: upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-assets
          path: dist/
          if-no-files-found: error

  publish:
    name: GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: release-assets
          path: release/
          merge-multiple: true

      - name: checksums
        run: (cd release && sha256sum * > SHA256SUMS)

      - name: create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
